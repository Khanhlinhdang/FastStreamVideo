# Place Cloudflare Origin Certificate files here when not using Caddy/Let's Encrypt:
#   cert.pem
#   key.pem
# Mount in docker-compose proxy service (see docs/CLOUDFLARE.md).
# Do not commit real certificates or private keys.
