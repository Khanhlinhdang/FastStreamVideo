# LiveStream Web

Frontend React + Vite + TypeScript cho nền tảng xem phim local **LiveStream**.

## Chạy local

```bash
cd web
npm install
npm run dev
```

Mở http://localhost:5173 — Vite proxy `/api` và `/media` → `http://localhost:4000`.

## Build

```bash
npm run build
```
